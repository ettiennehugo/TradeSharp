using TradeSharp.Common;
using TradeSharp.Data;

namespace TradeSharp.CoreUI.Repositories
{
  /// <summary>
  /// Concreate data store interface to query and edit instrument data in an asychronous fashion so as to not tie up the UI thread.
  /// </summary>
  public interface IInstrumentDataRepository
  {
    //constants


    //enums


    //types


    //attributes


    //properties


    //methods
    //Task<IEnumerable<string>> GetDataProvidersAsync();

    //Task<IEnumerable<>> GetItemAsync(Guid id);
    //Task<IEnumerable<T>> GetItemsAsync();
    //Task<T> AddAsync(T item);
    //Task<T> UpdateAsync(T item);
    //Task<bool> DeleteAsync(T item);
  }
}
