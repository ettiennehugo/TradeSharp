<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="TradeSharp.WinCoreUI.Views.SessionsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:winui="using:CommunityToolkit.WinUI.UI.Controls">
  <StackPanel x:Name="m_main" Orientation="Vertical">
    <StackPanel x:Name="m_toolbar" Orientation="Horizontal">
      <AppBarButton Icon="Refresh" Command="{x:Bind ViewModel.RefreshCommandAsync}" Label="Refresh" IsCompact="True"/>
      <AppBarButton Icon="Add" Command="{x:Bind ViewModel.AddCommand}" Label="Add session" IsCompact="True"/>
      <AppBarButton Icon="Edit" Command="{x:Bind ViewModel.UpdateCommand}" Label="Update session" IsCompact="True"/>
      <AppBarButton Icon="Delete" Command="{x:Bind ViewModel.DeleteCommand}" Label="Delete session" IsCompact="True"/>
      <AppBarSeparator/>
      <AppBarButton Icon="Copy" Label="Copy session" IsCompact="True"/>
    </StackPanel>
    <winui:DataGrid x:Name="m_sessions" ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=TwoWay}" GridLinesVisibility="All" HeadersVisibility="Column" CanUserResizeColumns="True" AutoGenerateColumns="True" AutoGeneratingColumn="m_sessions_AutoGeneratingColumn"/>
  </StackPanel>
</UserControl> 