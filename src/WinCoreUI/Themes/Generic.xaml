<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation">
  <!--<Style TargetType="LogView" xmlns:local2="using:TradeSharp.WinCoreUI.Views">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="LogView">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->

    <!--<Style TargetType="local2:StockInstrumentView" xmlns:local2="using:TradeSharp.WinCoreUI.Views">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local2:StockInstrumentView">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
</ResourceDictionary>
