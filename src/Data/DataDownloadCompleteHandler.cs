namespace TradeSharp.Data
{
  /// <summary>
  /// Data download completed for a specific instrument and resolution.
  /// </summary>
  public delegate void DataDownloadCompleteHandler(object sender, DataDownloadCompleteArgs args);
}
