namespace TradeSharp.Data
{
  /// <summary>
  /// Delegate type for when an order is updated in an account.
  /// </summary>
  public delegate void OrderUpdatedHandler(object sender, OrderUpdatedArgs args);
}
